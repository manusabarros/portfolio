import React, { forwardRef } from "react";
import "./Skills.scss";

const Skills = forwardRef((props: any, ref: any) => {
    return <section className="Skills" ref={ref}></section>;
});

export default Skills;
